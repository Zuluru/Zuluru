<p><?php
printf(__('The name should be descriptive of the facility in general (e.g. the name of the school or complex where the %s are found).', true),
	__(Configure::read('ui.fields'), true)
);
?></p>
